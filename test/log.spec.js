import { logError } from '../src/log'

describe('Log module', () => {
  describe('startCommand()', () => {
    it('should return an end function')
    it('should print a starting line')
    it('should finish and print a reasonable time')
  })
})
